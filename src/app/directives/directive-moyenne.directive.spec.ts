import { DirectiveMoyenneDirective } from './directive-moyenne.directive';

describe('DirectiveMoyenneDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveMoyenneDirective();
    expect(directive).toBeTruthy();
  });
});
